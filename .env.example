# Transport selection. `mock` (default) uses the local simulator in api/mock/.
# Set to `http` once the backend exists; api/http.ts is already written.
EXPO_PUBLIC_API_MODE=mock

# Only read when EXPO_PUBLIC_API_MODE=http.
EXPO_PUBLIC_API_BASE_URL=https://api.taraz.app/v1
